extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable"])

type Query {
  ping_342: String
}

type User @key(fields: "id") {
  id: ID!
  field_342: String
}
